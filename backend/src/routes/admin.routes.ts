import { Router } from 'express';

const router = Router();

// Placeholder for admin routes: auth, catalog CRUD, application queue, audit logs
// Actual domain implementation will be added in Phase 7

export default router;
