import { Router } from 'express';

const router = Router();

// Placeholder for EMI routes: GET /api/v1/variants/:variantId/emi-plans
// Actual domain implementation will be added in Phase 4

export default router;
